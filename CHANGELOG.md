# 변경 이력

DevFlow의 기능 변경 이력입니다. 제품 개요·설치 방법은 [README](README.md)를 보세요.

> 항목 끝의 `(W)` `(V)` `(T)` 같은 기호는 내부 작업 배치 코드로, `HANDOFF.md`의 세션 기록(9-W, 9-V …)과 대응합니다.

---

## 최근 업데이트 (최신순)

- **MCP 태스크 수정·일괄 도구 + 캘린더 스코프(AA)** — 실사용(WBS 499행 MCP 일괄 등록)에서 나온 개선 요청서 5건 중 즉시 가능한 1묶음. ① Claude가 잘못 넣은 제목·설명·날짜를 **`update_task`로 수정**(null=비우기 — "예정일만 지우고 마감일은 유지" 가능), **`bulk_create_tasks`/`bulk_update_tasks`**로 최대 200건 한 번에(부분 실패 허용 — 어느 건이 왜 실패했는지 반환). 79건 예정일 일괄 제거가 호출 한 번이 된다. ② `create_task`·bulk에 **상위 태스크 지정**(parent_task_id, bulk에서는 요청 안 임시 키 `parent_ref`로 WBS 계층째 등록) + 날짜 YYYY-MM-DD 엄격화(하루 밀림·롤오버 차단). ③ `list_events`가 **기본으로 해당 프로젝트 일정만** 반환(개인·전체는 scope 명시) — LLM 무스코프 호출로 "등록 안 한 일정이 보인다" 오해하던 근원 차단. ④ **사이드바 미니 달력 점도 활성 프로젝트+개인 일정만** — 점 찍힌 날을 눌렀는데 아무것도 없는 모순 제거. 멀티에이전트 반박검증(18에이전트) 확정 7건 수정 — **휴지통 프로젝트 콘텐츠가 집계형 읽기 6경로(My Work·캘린더·AI 검색·MCP 목록/검색/일정)에 유령처럼 남던 잔여 격차 전수 봉합**(task_id 직통 경로 포함), bulk 미지원 필드 조용한 무시 거부(REST와 동일 규약), 실패 항목 ref 재사용 차단, 비멤버 프로젝트 존재 오라클 봉합 등. 테스트 98개. 스키마 무변경 — 배포는 pull/Republish만, Claude 커넥터 재연결 시 새 도구 반영
- **프로젝트 보관·휴지통(30일)·영구 삭제(Z)** — 끝난 프로젝트를 정리할 방법이 아예 없어 목록이 계속 쌓이던 것을, 프로젝트 카드 `⋯` 메뉴의 **보관하기 / 삭제…**로 해결. 삭제는 **문서·회의록 휴지통과 같은 규약** — 30일 안에는 복원할 수 있고, 기다리기 싫으면 휴지통에서 즉시 영구 삭제. 삭제 다이얼로그가 **무엇이 사라지고 무엇이 남는지**를 먼저 보여준다(태스크·문서·회의록·일정·스니펫은 삭제 / 게시된 노하우·갤러리 제출물은 **보존**, 프로젝트 연결만 끊김) + 프로젝트명 타이핑 확인(서버도 동일 강제). **검수 전 노하우 초안**이 있으면 경고하되 **막지는 않는다** — 소유자가 게시하지 않고 떠나면 아무도 못 지우는 교착이 생기기 때문. 프로젝트가 사라져 `project_id`가 NULL이 된 **고아 노하우**는 작성자·사이트 관리자에게 계속 보인다(프로젝트는 끝나도 배움은 남는다). 사이트 관리자는 멤버가 아닌 프로젝트도 정리 가능. 영구 삭제 사실은 `project_deletions`에 스냅샷으로 남는다(activity_log는 프로젝트와 함께 지워지므로). 멀티에이전트 반박 검증(33 에이전트)에서 확정 14건, 이어 미검증 low 9건 전수 재확인으로 7건 추가 수정 — **파괴적 라우트 Bearer 차단(C5)**, **휴지통 프로젝트 읽기·쓰기 차단**(`requireMember`), **고아 초안 상세·내보내기 권한 단락 수정**(비공개 초안 전문 노출), AI 색인 제외, 관리자 승격 열람 세션 전용화, 크론 기동 시 따라잡기·배치 상한, 감사 주체 오귀속(자동/수동 구분), 영향 집계 실패 시 삭제 경로 봉쇄 등. 배포 시 `npm run db:push` 필요(projects.deleted_at·deleted_by, project_deletions)
- **비밀번호 찾기 — 메일 재설정(Y)** — 로그인 화면에 **「비밀번호를 잊으셨나요?」**가 없어 비밀번호를 잊으면 들어올 방법이 없던 것을 해결. 메일로 재설정 링크를 받아 스스로 바꾼다(2시간 만료·1회용·해시 저장). **계정 유무를 드러내지 않는다** — 가입 여부와 무관하게 같은 응답·같은 소요시간(OWASP 열거 방지). 재설정하면 **그 사용자의 세션과 API 토큰을 전부 폐기**하고 계정 잠금도 함께 풀린다. 로그인 상태에서는 설정 화면에서 현재 비밀번호 확인 후 변경. SMTP 미설정이면 조용히 꺼지고, 발송이 실패해도 요청은 성공으로 끝난다(실패 여부가 계정 존재를 알려주면 안 되므로). 메일이 막힌 상황을 위해 **관리자 발급 링크**(세션 전용)와 **CLI 스크립트**(`npm run reset-password` — 관리자 본인이 잠긴 경우)도 함께. 멀티에이전트 반박 검증 확정 6건 수정 — Bearer 토큰 우회(C5), **Host 헤더 링크 포이즈닝**(`APP_BASE_URL` 고정+HTML 이스케이프), 재설정 후 API 토큰 잔존, CLI 개발용 시크릿 무음 실패 등. 배포 시 `npm run db:push` 필요(password_resets)
- **README 방문자용 재구성 + 이력 분리(X)** — README 313줄 중 3분의 1이 세션 단위 업데이트 로그(31항목, `(W)` 같은 내부 배치 코드 노출)라 처음 온 사람이 제품을 파악할 수 없던 것을, **이력 전량을 이 파일로 옮기고**(손실 0 대조 검증) README는 방문자 동선으로 재편 — 라이브 데모 CTA, `문서→할일→가이드→노하우` 핵심 루프 다이어그램, 기능 표 11행, 가장 쉬운 실행법(`npm run dev:ui`) 전면 배치, 설치·배포 상세는 접기. 첫인상 분량 313→167줄. 테스트 개수 표기 불일치(배지 79 vs 본문 77)를 실측 79로 통일하고, CLAUDE.md 워크플로의 문서 갱신 위치를 CHANGELOG 기준으로 동기화. 문서 전용(코드·스키마 무변경)
- **내 기록 — 빈 날은 안 남기고, 이미지-only 날은 표시(W)** — 텍스트를 썼다 지워 **내용도 이미지도 없는 날**이 월 목록에 '유령 점'으로 남던 것을, 저장 시 행을 정리하고 **조회에서도 걸러**(수정 배포 전에 이미 쌓인 빈 날까지) 사라지게 하고, **이미지만 올린 날**은 목록에 `📷 이미지 N장`으로 명시(빈 날처럼 안 보이게). 마지막 이미지를 지우면 앵커 행도 자동 정리. 멀티에이전트 반박 검증(9에이전트)으로 확정 결함 3건(업로드 히트맵 갱신 누락·이미지 앵커 본문 덮어쓰기 경합·삭제 비원자 TOCTOU) 전부 수정. 테스트 79개
- **큰 모니터에서 화면 꽉 채우기 — 반응형 풀폭(V)** — 콘텐츠 폭이 1536px에 고정돼 있어 32인치 등 큰 모니터에서 좌우 여백이 크게 남고 캘린더 주간 뷰가 가운데로 쪼그라들던 것을, **뷰포트 폭에 맞춰 꽉 차게(풀폭)** 바꿈. 캘린더·보드·리스트가 넓은 화면을 그대로 활용(16인치 이하는 기존과 동일). Layout `<main>`의 max-width 상한 제거 — 클라 1파일, 서버·스키마 변경 없음
- **문서 파생 태스크의 「출처 문서 보기」를 상단으로(U)** — 문서에서 분해·파생된 태스크에서 원본 문서로 돌아가는 지름길인 '출처 문서 보기' 링크가 설명 카드 밑 **회색 pill로 묻혀** 특히 넓은 웹 화면에서 잘 안 보이던 것을, 태스크 상세 **최상단(태스크 번호 줄 우측)**으로 올리고 **버튼형(브랜드 톤 채색 + 사각 모서리 — '승인'과 같은 버튼 언어)**으로 바꿔 문서→태스크→문서 왕복을 한 번에. 문구·동작은 그대로(그 태스크의 출처 문서로 이동, 문서 파생 태스크에만 노출), 서버·스키마 변경 없는 클라 1파일 개선
- **타임라인 "화면 폭 = 한 달" + MCP 프로젝트 기간 도구(T)** — ① 월 (일별) 모드의 하루 폭을 고정값 대신 **화면 폭에 맞춰 동적 계산**해 어떤 화면에서든 정확히 한 달이 차게(하한 28px — 좁은 화면·모바일은 가로 스크롤), 캔버스를 월 경계에 정렬하고 **◀▶·오늘·전체 모드 클릭 점프가 전부 달력 넘기듯 월 1일로 스냅**. ② MCP에 **`update_project_dates` 도구 신설**(19종): Claude가 프로젝트 기간을 설정·변경·해제(owner/manager 전용), `list_projects`도 기간을 반환. ③ 서버 검증 보강: 프로젝트 생성·수정 API가 종료<시작을 400으로 거부(부분 수정은 기존 값과 병합 판정), MCP 날짜는 YYYY-MM-DD 엄격 검증(하루 밀림·롤오버 차단), 생성 API의 null→1970 zod 함정 수정. 멀티에이전트 리뷰(13에이전트) 확정 9건 전부 반영, 테스트 에러 경로까지 확장(77개 유지)
- **타임라인 「월 (일별) | 전체」 배율 탭 + 프로젝트 기간(S)** — 타임라인에 배율 세그먼트 탭: **월 (일별)** 모드(기본)는 가로 스크롤을 유지하며 **날짜 숫자를 매일 표기**(토=파랑·일=빨강·오늘=보라 원)하고 주말 열에 음영, **전체** 모드는 프로젝트 기간 전체를 화면 폭에 압축해 스크롤 없이 조망(막대 얇게, 빈 곳 클릭 시 그 날짜의 월 보기로 점프, 선택 모드 기억). 그리고 **프로젝트 기간(시작~종료)**을 드디어 넣을 수 있음 — DB·API는 원래 지원했는데 입력 UI가 없었다: 새 프로젝트 모달의 「기간 (선택)」, 보드 헤더 프로젝트명 아래 표시+연필 수정(매니저 이상, D-day 표시), 프로젝트 카드에 기간 배지. 전체 모드에서 프로젝트 기간이 **연보라 띠 + 시작/종료 플래그**로 깔려 "전체 일정 중 어디쯤"이 보인다
- **태스크 추가할 때 기간까지 한 번에(R)** — 보드 「+ 태스크」 폼이 **통합 폼**으로: 연회색 패널 안에 제목 / **예정~마감 기간**·담당자·우선순위 / 설명(선택)이 전부 보이고(숨어 있던 "설명·우선순위 ▼" 토글 제거), 그 아래 큰 [추가] 버튼(웹 오른쪽 정렬, 모바일 전체 폭 — 위에서 아래로 채우고 마지막에 실행). 태스크를 만들고 상세 화면에 들어가 날짜를 다시 잡던 2단계가 사라짐. 예정일 기본값은 오늘(비우면 "날짜 미지정" 트레이로), 마감일은 예정일보다 앞선 날짜 선택 불가, 연속 추가 시 날짜·담당자·우선순위 유지(같은 날 여러 개 빠르게). MCP `create_task`에도 `due_date` 추가(예정일 역전 거부 — REST와 동일 규칙), 티켓 요청 모달의 "희망 마감일"은 기존 그대로
- **정렬 규약 — 리스트 등록순 + 캘린더 "나 먼저"(Q)** — ① **태스크 목록이 등록순**(먼저 등록한 것이 위)으로 바뀜: 문서에서 분해한 태스크가 문서 순서(로드맵 순)대로 보인다(기존엔 최신이 위라 역순으로 보였음). 드래그로 정한 순서는 여전히 최우선이고, MCP `list_project_tasks`도 같은 순서(Claude가 받는 순서 = 화면 순서). ② **캘린더 주간·일 뷰에서 내 열이 항상 맨 앞** + 이름 옆 "나" 배지 — 모바일에서 내 일정 찾으러 가로 스와이프하던 것 제거(Google 캘린더·Outlook 일정 도우미와 같은 관례). 나머지 팀원은 가입순이라 각자의 화면에선 매번 같은 순서. 같은 규약을 팀원 필터 칩·빠른 추가 담당자·태스크 담당자/일정·회의록 참석자 픽커에도 일괄 적용. ③ **멤버 목록 서버 정렬 고정**(가입순) — ORDER BY 부재로 역할 변경 후 화면 순서가 뒤바뀔 수 있던 잠재 버그 수정. 정렬 회귀 테스트 2건 추가
- **태스크 운영성 3종 + 재분해 diff(P)** — ① **태스크 제목·체크리스트 문구 인라인 수정**: 상세 화면에서 연필 아이콘으로 바로 수정(제목=매니저, 체크 문구=담당자·매니저 — 피드백 달린 항목을 삭제 없이 오타 교정). ② **리스트·칸반 순서 드래그**: 매니저가 카드를 끌어 같은 상태 그룹 안에서 순서 변경 — 카드 위 드롭=그 위로 끼워넣기, 빈 곳 드롭=맨 아래(사이값 부여, 간격 소진 시 자동 재번호). ③ **문서 재분해가 diff로**: 문서를 고치고 다시 분해하면 [새 태스크로 만들기 / 이미 연결됨(문서에 새로 생긴 체크 항목만 기존 태스크에 병합) / 문서에서 사라진 항목 안내(자동 삭제 없음)]로 나뉘어 표시. 매칭은 3단 — 반영 시 저장하는 **앵커(원본 분해 제목)** 정확 일치 → 제목 유사도(단어 추가형 개정 커버) → LLM 판정(키 등록 시, 애매한 잔여쌍만). 태스크 제목을 앱에서 바꿔도, 문서 쪽 제목이 개정돼도 연결이 유지된다. 멀티에이전트 검증 확정 8건 수정(반영 원자성 — 병합 대상 선검증으로 "실패 시 쓰기 0건" 보장, 앵커 드리프트 2종, 30개 절단 시 오탐 안내, Firefox 드래그 이탈 등). 배포 시 `npm run db:push` 필요(source_anchor 컬럼)
- **내 기록 v1.5·v2(N)** — ① **이미지 자동 텍스트 추출(OCR)**: 첨부한 스크린샷·사진에서 LLM 비전이 텍스트를 뽑아 검색 대상에 포함(관리자 화면에 LLM 키 등록 시 동작, 전송 전 축소·투명 PNG 흰배경 보정·업로드 rate limit). ② **AI 검색에 "내 기록" 포함**: 전체 검색·질문하기의 근거에 본인 저널이 섞여 나옴(출처에 날짜 링크) — **앱 로그인 전용**으로, API 토큰은 스코프와 무관하게 저널에 닿지 못함(검증단 발견 보안 픽스). ③ **요약 버튼**: 그날 완료한 태스크·참석 일정을 본문에 자동 프리필 — 나는 코멘트만. ④ **조각 승격**: 드래그한 텍스트를 태스크/문서로(프로젝트 선택, 멤버 권한이면 티켓 안내). ⑤ **주간 모아보기**(그 주 기록 한 화면+전체 복사) ⑥ **잔디 히트맵**(최근 16주, 진할수록 많이 쓴 날 — 스트릭 카운터는 의도적으로 없음) ⑦ **작년 오늘** 카드 ⑧ **🎤 음성 입력**(브라우저 음성 인식, 지원 기기만 버튼 노출) ⑨ **안드로이드 공유 대상**: 다른 앱에서 공유→DevFlow 하면 내 기록에 저장(서비스워커 수신 — 크로스사이트 주입 차단, 로그인 후 경로 복귀, 부분 실패 재시도에 중복 저장 없음. iOS는 플랫폼 미지원 — 시리 단축어 경로 사용). 멀티에이전트 검증으로 확정 결함 14건(보안 1·데이터 유실 2 포함) 출시 전 수정. 배포 시 `npm run db:push` 필요(ocr_text 컬럼)
- **알림 신뢰성 + 리마인드 설정 + 내 기록 v1(N)** — ① **알림 신뢰성**: autoscale 배포에서 서버가 잠들면 매분 크론이 멎어 리마인더·다이제스트가 유실되던 문제를 3중 보완 — 요청 유입 시 밀린 알림을 따라잡는 기회주의 tick(1분 스로틀), 외부 무료 크론 핑(5분, `GET /api/health`) 안내, sendOnce를 "키 선점→발송, 실패 시 회수" 선점형으로 바꿔 동시 실행 중복 봉인(일시 장애는 재시도, 중복 발송보다 1회 유실을 선택). ② **일정별 리마인드**: 일정 등록·수정 팝업에서 시점 선택 — 시간지정(기본 30분 전/10분/1시간/하루 전/없음), 종일(없음=다이제스트/당일 아침 9시/전날 저녁 9시). 일정을 연기하면 새 시각 기준으로 다시 알림(멱등 키에 시각 포함). ③ **내 기록 v1**: 라이브러리에 "내 기록" — 하루 = 한 페이지(열면 오늘 장, 1.5초 자동저장), "+ 지금" 시각 스탬프, `#태그` 자동 추출·검색, 이미지 붙여넣기·버튼 첨부(원본 보존+썸네일), 월 달력에 쓴 날 점, **완전 비공개**(모든 쿼리 본인 필터 — 관리자 우회 없음, 전용 `journal:write` 토큰 스코프로 다른 토큰과 격리). MCP `journal_append`/`journal_search` 추가(도구 18종) — 클로드 앱에서 "기록해줘"로 저장, 작업 중 검색 활용. 시리 단축어용 `POST /api/journal/append`(API 토큰). 부수: **모바일 하단 탭바 스와이프** — 탭 8개가 한 줄에 눌려 작아지던 것을 탭당 최소폭 보장+옆으로 넘기기(스크롤바 없이 가장자리 페이드, 이동 시 활성 탭 자동 따라옴)로 교체, 탭바형 가로 줄(프로젝트 탭바 등)의 스크롤바도 일괄 숨김
- **아바타 색 개편 + 가로 스크롤 ‹ › + 프리뷰 파일 불러오기(B)** — 아바타 색을 이름 해시(9색, 실팀에서도 겹침)에서 **가입 순번 기반 18색**으로 바꿔 조직 18명까지 전원 다른 색을 보장하고, 역할은 색과 분리해 아바타 모서리의 **알파벳 배지**로 표시(소유자=금색 C, 매니저=은회색 M, 흰 외곽선 — 링·도형 시안과 실물 크기 비교 후 선택, 툴팁에 역할). 가로로 넘치는 화면 5곳(캘린더 주간·일·타임라인, 프로젝트 탭바, 태스크 상세 탭줄)에 **‹ › 화살표** — 넘칠 때만 보이고, 클릭=한 화면·꾹 누르면 연속 이동, 표가 길어도 보이는 영역 중앙을 따라오며, 모바일에선 스와이프 가능하다는 신호 역할(탭바는 가장자리 페이드 겸용). **프리뷰**는 "파일 추가"를 "새 파일(빈 파일)"과 "**파일 불러오기**(내 PC 코드·텍스트 파일, 여러 개 선택·드래그앤드롭, 같은 이름은 덮어쓰기 확인)"로 분리. 부수: 로컬 개발 모드에서 로그인 칸에 시드 계정 자동 입력(배포 빌드엔 미포함 — 번들 검사로 확인)
- **팝업 위치 버그 근본 수정 + 참석자 칩 접기(B)** — 팝업이 화면이 아니라 **페이지 전체 기준**으로 떠서(모바일에서 문서 중간·바닥에 뜨고 열릴 때 화면이 저절로 스크롤) 생기던 버그의 근본 원인을 수정. 페이지 등장 애니메이션(`fill-mode: both`)이 본문에 transform을 영구 잔존시켜 내부 `position: fixed`의 기준을 바꿔버린 것 — fill을 `backwards`로 바꾸고, 팝업류(Modal·확인·입력창)를 **React Portal**(`body` 직속)로 옮겨 앞으로 어떤 조상 애니메이션이 추가돼도 재발하지 않게 함(헤더·하단 탭바도 이제 정상적으로 어두워짐). **일정 참석자 픽커**는 팀원 9명 이상이면 전원 선택+5명+`+N`(총 7칩, 실측 기준 모바일 2~3줄)로 접힘 — 참석 선택된 사람은 항상 노출, 일정 팝업·보기 전용 목록·회의록 승인 3곳 동일 규약. 부수: PGlite 로컬 실행기(`scripts/dev-pglite.ts`) 추가 — DB 없이 UI 확인·검증용
- **상태 그룹 접기 + 모바일 팝업 중앙 배치(B)** — 리스트 상태 그룹과 칸반 컬럼 헤더를 **클릭해 접기/펼치기**(회의록 월 접기와 같은 규약, 접힘은 기기에 기억 — "완료는 늘 접어둠" 유지, My Work 칸반 포함). 모바일에서 칸반 컬럼이 세로로 쌓여 '진행 중'까지 한참 내려야 하던 문제의 해법. 접힌 컬럼도 드래그 중에는 임시로 펼쳐져 드롭 가능. **모바일 팝업**은 화면 아래 붙는 바텀시트에서 **중앙 배치**로 바꿔 긴 폼도 제목·첫 입력칸부터 보임. **팀원 필터 칩**은 9명 이상 팀에서 앞 7명 + `+N` 접기로 한 줄 고정(선택된 팀원은 항상 노출, 8명 이하는 변화 없음)
- **보드 상단 재배치 + 타임라인 개편** — 상단 컨트롤을 역할별로 4줄로 정리(탭 바+완료·추출 / 타이틀+`+태스크`·`+일정` / 팀원 필터+뷰 스위처 / 캘린더 도구줄). 태스크 추가 폼은 `+태스크`로 접고, `+일정`은 리스트·칸반 등 어느 뷰에서도 열리게 부모 레벨로 승격. 캘린더 도구줄은 범례(알약)와 눈금(세그먼트)을 모양으로 구분하고 "이번 주/오늘부터 7일" 토글 제거(이번 주 고정). **타임라인**은 전체 기간을 화면에 압축하던 방식을 **하루 폭 고정 + 가로 스크롤**로 바꿔 프로젝트가 길어도 날짜가 또렷하고(2단 축: 월 크게+주 눈금), 오늘 세로선·진입 시 오늘 자동 스크롤·오늘/‹›(한 달) 이동 추가. **리스트 뷰**는 세로 카드가 넓은 화면에서 빈 공간이 크던 문제를 한 줄 행으로 교체
- **보드 빠른 추가 한 줄 복원** — 공용 Select의 기본 폭(100%)이 호출부 지정 폭(w-auto)을 이겨 담당자 선택이 한 줄을 통째로 먹던 충돌 수정(9곳 일괄 정상화: 보드 담당자·우선순위, 태스크 상세 담당자·선행 추가, 회의록 반영 대상, 갤러리 카테고리). 보드가 `[제목] [담당자] [설명·우선순위 ▾] [+ 추가]` 한 줄이 되고(입력 → 마지막에 추가 순서), 잘 안 보이던 "설명·우선순위 추가" 링크는 테두리 버튼으로 승격
- **삭제 보호: 휴지통·버전 기록(B)** — 문서·회의록 삭제는 **매니저 전용**이 되고, 지워도 바로 사라지지 않고 **휴지통**으로 이동(매니저가 복원·영구 삭제 — 영구 삭제는 휴지통 경유 2단계 강제). 문서는 내용이 바뀌는 저장마다 직전 본문을 **버전 기록**(최근 20개)으로 남겨 "누가 내용을 다 지웠다" 사고도 복원 가능(에디터 "버전" 버튼). 스니펫 수정은 작성자·매니저만(삭제와 동일 게이트), 첨부파일·체크리스트 삭제에 확인창 추가, 삭제 버튼 노출을 서버 권한 규칙과 일치화
- **파일로 회의록·문서 작성(B)** — `.txt/.md` 파일을 **버튼 또는 드래그앤드롭**으로 불러와 새 회의록·문서(파일로 새 문서)·문서 편집기에 채움. 브라우저에서 내용만 읽는 방식이라 서버 변경 없음, EUC-KR 옛 메모장 파일 자동 감지, 기존 문서 덮어쓰기는 확인창
- **캘린더 빠른 일정(B)** — 도구줄을 `+ 일정(보라) → 오늘 → 날짜 이동` 순으로 재배치하고, 주간·월간·일 뷰 **칸에 마우스를 올리면 ➕ 버튼**이 나타나 그 날짜(주간·일 뷰는 그 팀원 참석까지) 프리필된 일정 팝업이 열림 — 기존 칸 클릭 동작은 그대로. 사이드바 미니 달력은 다른 달을 볼 때 **"오늘" 복귀 버튼** 표시
- **팝업 UX(B)** — 팝업 안에서 텍스트를 드래그하다 바깥에서 손을 떼도 **팝업이 닫히지 않음**(백드롭에서 누르고 뗄 때만 닫힘), 새 회의록·태스크 분해 팝업 확대(넓은 팝업 + 내용칸 화면 높이 45%), 한글이 단어 중간에서 줄바꿈되지 않게 전역 규칙(keep-all) 적용
- **프로젝트 탭 바(C12)** — 보드·문서·회의록·프리뷰·팀원 탭이 모든 프로젝트 화면 상단 같은 자리에 고정("보드로 돌아가서 다시 들어가는" 왕복 제거, 팀원 수 배지 포함). 회의록 목록·문서 트리는 **화면 높이에 가둬 내부 스크롤**(데스크톱은 sticky) — 쌓여도 페이지가 무한정 길어지지 않음
- **만든 사람 표시 일원화(C13)** — 태스크에만 있던 "만든 사람"을 전 콘텐츠로 확장: **회의록**(목록·상세 등록자 칩), **문서**(에디터에 만든 사람·등록일, 다른 사람이 고쳤으면 마지막 수정자, 트리 툴팁), **일정**(모달에 만든 사람·등록일 — 대리 등록 판별), **프리뷰 스니펫**(칩 툴팁)
- **회의록 목록 월별 구분 + 접기(C14)** — 목록이 `2026. 7 · 5건` 같은 **월 헤더**로 묶이고 스크롤 중 상단에 고정(sticky). 헤더 클릭으로 접기/펼치기 — **기본은 최신 월만 펼침**이라 백 건이 쌓여도 한 화면. 검색 중엔 전부 펼쳐 결과가 가려지지 않음
- **잔여 이슈 일괄 해소(R4) + 최종 회귀 검토 2라운드(C5·C6)** — 보안 4건(REST 스코프 게이트·세션 고정 방어·URL 스킴 검증·토큰 자기승격 차단) + **타임라인 일정 마커(◆)** + 미니 달력 점 표시 + 주간 뷰 일정 띠 + 야간 일정 + 승인 착수일 + My Work 보강. 이후 독립 검증단이 수정분을 재검토해 **멀티데이 일정 수정 시 기간 파괴** 등 26건을 추가 확정·수정 (전 라운드 미해결 0 확인)
- **일정 수정·삭제 + 캘린더 전면 감사** — 일정 칩을 **클릭해 수정·삭제**(권한 없으면 보기 전용), 멀티데이 일정 기간 표시, 날짜 미지정 트레이(끌어서 배치), 칸반·타임라인·리스트 확정 이슈 20건 일괄 수정 (개발 일지 R3 참조)
- **일정 "주인" 규약(C9)** — 참석자 = 일정의 주인·알림 수신자. **대리 등록**(`include_creator:false` — "제윤이 일정 잡아줘"가 제윤 열로), MCP·회의록 승인에 **참석자 지정**(화자 자동 제안), 4개 등록 경로가 공용 규칙(eventService) 하나로 통합. 태스크 상세에 **만든 사람·등록일** 표시
- **일정 배치 규칙(주간·일 뷰)** — "일정은 항상 누군가의 것": 개인·단독(참석자 미지정=만든 사람)·일부 참석 일정은 **참석자 각자의 열에**, 공통 띠는 **전원 참석 일정만** (참석자 픽커에 "전원 선택" 버튼)
- **프리뷰 코드펜식 개편** — 좌측 파일별 다크 에디터 스택 | 우측 라이브 결과 나란히, **타이핑 멈추면 자동 실행**(0.8s), 파일 타입 컬러 탭, 스니펫은 상단 칩으로
- **회의록·문서 탐색 개선** — 회의록에 **회의 날짜**(입력·표시·정렬·수정), 업로드 폼을 모달로 분리(목록이 주인공), 회의록·문서 트리에 **검색** 추가
- **타임라인 담당자 색상 칩** — 사람별 고정 색(아바타와 동일)의 2글자 이름 칩을 태스크 행·막대에 표시 — 누가 맡았는지 색으로 한눈에
- **사이드바 화면 고정** — 본문이 길어도 미니 달력·설정·로그아웃이 항상 보임 (메뉴만 내부 스크롤)
- **캘린더 카드 드래그 이동 + 할 일/일정 필터** — 주간/일 뷰에서 카드를 끌어 요일(예정일)·팀원(담당자) 이동, 범례가 `전체/할 일/일정` 토글로 동작

---

## 개발 일지

빌드 스펙 `devflow-build-prompt.md`의 **P0~P10 전 단계 + 후속 로드맵 상당 부분**을 구현했습니다.

### MVP (P0~P5) ✅

| Phase | 내용 |
|-------|------|
| P0 | 모노레포·Vite·Express·Drizzle·docker-compose(app+pgvector+MinIO)·세션·모바일 하단탭바 셸·pgvector 활성화 |
| P1 | 초대 토큰 인증·프로젝트/멤버십(프로젝트별 역할)·api_tokens 발급/폐기·계정잠금·열거방지 |
| P2 | tasks(item_key 원자 생성)·복수 담당·상태/완료·체크리스트·서브태스크 롤업·My Work·List/Kanban/Calendar |
| P3 | Updates 패널(스레드 댓글·마크다운 sanitize)·is_guide → guide_assignees(팀원별)·진행률 배지·미수행 가이드 집계 |
| P4 | S3호환 스토리지 어댑터·magic-number 검증·sharp 썸네일·인가 후 presigned/스트리밍·web-push·알림 멱등성·cron |
| P5 | 프로젝트 완료 → applied 가이드·해결 blocker·skipped(안티패턴) 수집 → SKILL.md 초안 → 검수 후 게시·내보내기 |

### 후속 (P6~P10) ✅

| Phase | 내용 |
|-------|------|
| P6 | Timeline(Gantt-lite) + task_dependencies(사이클 방지). 보드 "타임라인" 뷰 + 태스크 상세 선행 태스크 관리 |
| P7 | AI RAG — 인제스트(embedding_jobs)→검색→Q&A→가이드 제안(사람 검토). 프로바이더 mock/openai 교체형 |
| P8 | GitHub 웹훅(X-Hub-Signature-256 검증·webhook_events 멱등·저장소↔프로젝트 바인딩·item_key 파싱)·PR머지 자동완료 가드레일 |
| P9 | 라이브 프리뷰 — sandbox iframe(same-origin 금지)+CSP·멀티파일 스니펫·JSX는 esbuild-wasm |
| P10 | MCP 서버 — `/api/mcp` JSON-RPC, api_tokens Bearer+스코프, 초기 도구 6종 |

**로드맵 반영분**

- 관리자 설정: LLM 프로바이더/키 UI 관리(AES-256 암호화·마스킹, 재시작 없이 반영) — 사이트 관리자 전용
- 회의록 → AI 구조화: 결정/실행/가이드/블로커/질문 추출 제안 → 사람 검토 후 반영
- P11 검증 갤러리 + 공개 가입: 완료 프로젝트 제출 → 리뷰·평점 → 게이트 충족 시 승격 (§10.2 개정: 가입 공개, 프로젝트는 초대만)

### R1~R2 팀 워크플로우 확장 및 이후 배치 ✅

| 기능 | 내용 |
|------|------|
| 티켓 시스템 | member가 "티켓 요청"(requested) → 매니저 승인(담당자 배정·가이드 백필)/반려(사유 필수·댓글 이력). 일반 PATCH로 requested/rejected 전이 불가 |
| 문서 페이지 | 프로젝트별 마크다운 문서 트리(자동저장·서버 sanitize 렌더). 미리보기에서 텍스트 선택 → 태스크/티켓 파생(출처 역추적) |
| My Work 칸반 | 리스트/칸반 토글 + 내 요청 티켓 포함 + 상태·마감·주간 완료 시각화(순수 CSS) |
| 캘린더 UX | 오늘 행/셀 강조 + 자동 스크롤, "이번 주/오늘부터 7일" 토글, 날짜 규약 통일(client/src/lib/format.ts) |
| 일정 이벤트 | 개인/프로젝트 일정 + 참석자 + 30분 전 리마인더(멱등) + 캘린더 병렬 표시 + My Work 오늘 일정 |
| 보안 강화(R0) | 초대 링크 계정 탈취 차단(409) 등 R0 감사 픽스 일괄 |
| 역할 계층 | 프로젝트 역할 **소유자 > 매니저 > 멤버**(owner=창립자 1명, 매니저 권한 상속). 소유자는 강등·제거 불가, **소유권 양도**로만 이동. 사이트 관리자(is_admin)는 별개 전역 축 |
| R2 태스크 개편 | 상세 탭 재배치(체크리스트 기본·설정 탭)+설명 상시 노출·편집·삭제, 빠른 추가에서 담당자·설명·우선순위 즉시 지정 |
| R2 회의록 v2 | 원문 표시·수정/삭제 + 일정/체크리스트로도 반영(자동 등록 금지, 사람 승인) |
| R2 문서 분해 | 설계 문서 → 태스크+체크리스트 분해 제안(구조 기반, LLM 보강) → 검토 후 일괄 반영(WBS) |
| R2 MCP·설정 | 개인 API 토큰 발급/폐기 UI + MCP 연결 안내(`/settings?tab=mcp`). 도구 16종(일정 생성·조회 포함) — Claude가 설계 문서 등록→분해→배정→보고까지 수행 |
| UX·접근성 | 사이드바 미니달력 날짜 클릭 → 해당 날짜 일(day) 뷰 이동. 텍스트 밀림·흐린 대비 정리, 모바일 터치 타깃 확대, 삭제 버튼 터치 기기 상시 노출 |
| MCP OAuth 2.1 | 보호리소스/AS 메타데이터(RFC 9728/8414) + 동적 클라이언트 등록(RFC 7591) + PKCE(S256) + 서버렌더 로그인·동의 + 리프레시 로테이션. 액세스 토큰은 api_tokens 재사용. 개인 토큰(Bearer) 병행 |
| PWA 설치·배지·푸시 | manifest+아이콘+iOS 메타, 앱 아이콘 배지 = 오늘 내 할 일 수(포커스 갱신+푸시 badge 첨부), 설정에서 알림 켜기/테스트 |
| 캘린더 DnD·필터 | 주간/일 뷰 카드 드래그로 요일(예정일)·팀원(담당자) 이동(매니저, 미배정 열 포함, REST 재사용·멱등). 범례=전체/할 일/일정 필터 버튼. 설정 상위 탭 분리 |
| R3 일정·캘린더 감사 | 멀티에이전트 감사(31 에이전트)로 확정한 20건 일괄 수정 — **일정 칩 클릭 → 수정·삭제**(생성자/매니저 외 보기 전용), 멀티데이 일정 기간 표시(+계속/~종료 라벨), 타 프로젝트 일정 혼입 제거+개인 배지, **날짜 미지정 트레이**(끌어서 예정일 배치), 미배정 열 드래그 중 표시, 드롭 하이라이트 플리커 픽스, 모달 기본 날짜 stale 픽스+종료<시작 검증, 칸반 member 자기 태스크 드래그 허용, done 재드롭 completed_at 보존(서버), 마감<예정 거부(서버), 종일 일정 UTC 자정 규약 강제(REST·MCP date-only), 종일 초대 push 가짜 시각 제거, 타임라인(반려 제외·음수 기간 정규화·UTC 눈금·무날짜 안내), 리스트 반려됨 토글, 주간 그리드 sticky 날짜 열(모바일), 보드 헤더 줄바꿈 |
| R4 잔여 이슈 해소 | **보안**: REST 스코프 게이트(제한 토큰의 REST 전체 접근 차단 — 읽기/쓰기 계열 검사), 로그인 시 세션 재발급(session fixation 방어), 갤러리 demo_url http/https 화이트리스트, 관리자 llm_base_url https 강제. **일정**: 타임라인 일정 마커(◆·멀티데이 막대, 클릭 수정), 주간 뷰 일정을 행 전체 폭 띠로(제목 가독), 야간(자정 넘김) 일정 익일 종료 저장. **UX**: 미니 달력 할일·일정 점 표시, 티켓 승인 폼 착수일 지정(무날짜 증발 방지), My Work 반려·날짜 미지정 섹션, 문서 생성 prompt()→앱 다이얼로그, 터치 안내 문구 |
| N 알림·내 기록 | 알림 신뢰성(기회주의 tick+외부 크론 핑+sendOnce 선점형), 일정별 리마인드 시점(remind_minutes), **내 기록**(하루 한 장 개인 저널: 자동저장·시각 스탬프·태그·이미지+OCR·요약 프리필·승격·주간 모아보기·히트맵·작년 오늘·음성 입력·안드로이드 공유 대상·MCP journal 도구·시리 단축어 API), 모바일 하단 탭바 스와이프. 검증단 확정 25건(N6 11건+N7 14건) 출시 전 수정 |
| P 태스크 운영성 | 태스크 제목·체크리스트 문구 인라인 수정, 리스트·칸반 순서 드래그(sort_order 사이값+재번호), **문서 재분해 diff**(source_anchor 저장, 3단 매칭: 앵커→유사도→LLM, 신규/병합/사라짐 분류, 병합 대상 선검증으로 반영 원자성). 검증단 확정 8건 수정 |
| Q 정렬 규약 | 태스크 목록 등록순(sort_order desc→created_at asc→id asc, MCP 동일 규약), 캘린더 주간·일 뷰 "나 먼저"+나 배지(팀원 칩·담당자/참석자 픽커 일괄 적용, 공용 meFirst), 멤버 목록 ORDER BY 가입순 고정(순서 비보장 잠재 버그 수정). 정렬 회귀 테스트 2건 |
| R 태스크 추가 폼 | 빠른 추가 통합 폼 — 제목/기간(예정~마감)·담당자·우선순위/설명 상시 노출(토글 제거), 큰 [추가] 버튼(웹 우측·모바일 전폭), 예정일 기본 오늘·역전 방지·연속 추가 시 유지. MCP create_task due_date 추가(REST 동일 검증). Button lg 사이즈 신설 |
| S 타임라인 배율·프로젝트 기간 | 타임라인 「월 (일별)\|전체」 세그먼트 탭 — 월: 일 숫자 눈금·주말 음영(반복 그라데이션), 전체: % 배치로 화면 폭 압축·프로젝트 기간 연보라 띠+시작/종료 플래그·빈 곳 클릭→월 점프, localStorage 기억. 프로젝트 기간 입력 UI 신설(생성 모달·보드 헤더 인라인 수정+D-day·카드 배지) — 서버·스키마 변경 없음 |
| T 월 맞춤 배율·MCP 기간 도구 | 월 모드 하루 폭 동적 계산(화면 폭=한 달, 하한 28px)·월 경계 캔버스·◀▶/오늘/점프 월 1일 스냅(스크롤 폴백 포함). MCP update_project_dates 신설(19종)+list_projects 기간 반환+YYYY-MM-DD 엄격 검증, REST POST/PATCH 역전 400(병합 판정·병합쌍 통째 기록)·POST null→1970 수정, serverInfo 0.3.0. 멀티에이전트 확정 9건 반영, 스키마 변경 없음 |
| U 출처 문서 바로가기 | 문서 파생 태스크의 「출처 문서 보기」를 설명 밑 회색 pill → item_key 행 우측 **버튼형**(brand 톤 채색+rounded-lg — 배지 아닌 버튼 언어)으로 이동·강조. 넓은 웹에서 묻혀 안 보이던 문서 복귀 동선 개선. 문구·동작 불변(그 태스크의 출처 문서로 이동, 문서 파생 태스크에만 노출), 클라 1파일(TaskDetail.tsx)·서버·스키마 무변경 |
| V 반응형 풀폭 | 콘텐츠 max-width 상한(1536px, `max-w-screen-2xl`)+중앙정렬 제거 → 뷰포트 폭 꽉 채움. 32인치 등 대형 모니터의 좌우 여백 해소, 캘린더·보드·리스트가 넓은 화면 활용. Layout `<main>` 클라 1파일, 서버·스키마 무변경 |
| X README·이력 분리 | README의 세션 이력(업데이트 31항목 + 개발일지 40행)을 `CHANGELOG.md`로 전량 이관(자동 대조로 누락 0 확인), README는 방문자 동선으로 재편 — 히어로 CTA·핵심 루프 다이어그램·기능 표·`dev:ui` 전면·설치/배포 details. CLAUDE.md 워크플로 5번(문서 갱신 위치)을 CHANGELOG 기준으로 동기화, 테스트 수 표기 실측 79로 통일. `.md` 전용 변경 |
| W 내 기록 빈 엔트리 정리 | 내용·이미지 모두 없는 빈 저널 행을 남기지 않음(PUT 빈 저장 시 첨부 없으면 행 삭제, 마지막 첨부 삭제 시 고아 앵커 정리). 이미지-only 날은 빈 행을 앵커로 유지하고 월목록에 image_count로 `📷 이미지 N장` 표시. **조회 필터**(`hasRecordCond` — 본문 있음 OR 첨부 있음)를 월목록·히트맵·주간롤업에 적용해 **배포 전 남은 빈 앵커도 제외**. 멀티에이전트 검증 확정 3건 수정(업로드 heatmap invalidate·앵커 onConflictDoNothing 본문 보존·삭제 NOT EXISTS 원자화). 서버 API+행 삭제라 위험 축 검증, 스키마 무변경 |
| Y 비밀번호 재설정 | `password_resets`(해시 저장·2시간·1회용) + nodemailer 선택 인프라(`SMTP_*` 없으면 무음 비활성 — VAPID 규약과 동일). `POST /auth/forgot-password`(항상 동일 응답·토큰 무조건 생성으로 타이밍 균일화), `/reset-password`(원자적 조건부 UPDATE로 1회용 보장 → 비밀번호 교체 → 잔여 재설정 무효화 → **세션 전량 파기 + API 토큰 폐기** → 재로그인), `/change-password`(현재 비밀번호 확인). 관리자 발급 링크는 세션 전용(C5), CLI `npm run reset-password`. 메일 링크는 `APP_BASE_URL` 고정(Host 헤더 포이즈닝 차단)+HTML 이스케이프. 로그인·초대 화면 진입점, `/reset` 페이지, 설정 탭. 검증단 확정 6건 수정. 테스트 10건 |
| AA MCP 도구 확장·캘린더 스코프 | MCP 22종 — `update_task`(null=비우기)·`bulk_create_tasks`/`bulk_update_tasks`(상한 200·부분 실패·parent_ref 계층)·`create_task` parent_task_id+날짜 엄격화·`list_events` scope(기본 project — 무스코프 혼입 차단). 미니 달력 일정 점 활성 프로젝트+개인 스코프(활성 없으면 쿼리 끔). 검증단 확정 7건 — **휴지통 콘텐츠 잔존 6경로 전수 봉합**(loadTaskForUser 게이트 + liveProjectIds — My Work·캘린더·AI 검색·MCP 3도구), bulk 미지원 키 거부, 실패 ref 재사용 차단, 존재 오라클 봉합(멤버십 인지형 게이트), bulk 집계 오염 무해화. serverInfo 0.4.0, 테스트 98, 스키마 무변경 |
| Z 프로젝트 보관·휴지통·삭제 | `projects.deleted_at/deleted_by` 소프트 삭제(30일) + `project_deletions` 감사 스냅샷(projects 미참조 — cascade로 함께 지워지면 안 되므로). 라우트 `archive/trash/restore/purge`+`/trash`+`/deletion-impact`(리터럴 라우트를 `/:projectId`보다 먼저 등록), 이름 타이핑 확인 서버 강제, 30일 크론+**기동 시 따라잡기**(autoscale 자정 유실 대비)·배치 상한 50. 노하우·갤러리 제출물은 `ON DELETE SET NULL`로 보존하고 **고아 가시성**을 목록·상세·내보내기에 공용 헬퍼로 통일(작성자·관리자 세션 한정). 클라 `⋯` 메뉴·보관/휴지통 섹션·영향 요약 다이얼로그(집계 실패해도 삭제 가능). 검증단 확정 14건 + low 재확인 7건 수정 — Bearer 차단(C5)·`requireMember` 휴지통 차단·고아 초안 권한 단락·AI 색인 제외·활성 프로젝트 해제 등. 테스트 8건 |
